import styled from 'styled-components';

export const Wrapper = styled.section`
  // Positioning
  position: relative;
  align-self: flex-end;
  // Sizing
  width: 100%;
  padding: calc(var(--padding) * 2) 0;
  z-index: 15;
`;
