import styled from 'styled-components';

const Tag = styled.li`
  border-radius: 50px;
  color: var(--primary-accent-color);
  font-size: 0.85em;
  white-space: nowrap;
  text-overflow: ellipsis;
  text-align: center;
`;

export default Tag;
