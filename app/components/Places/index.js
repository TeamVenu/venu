import React from 'react';

export default class Places extends React.PureComponent { // eslint-disable-line react/prefer-stateless-function
  render() {
    return (
      <div>This is the Places Component</div>
    );
  }
}
